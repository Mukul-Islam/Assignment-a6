1) What is the difference between null and undefined?

=>null is like a zero value where is the undefined means is there is no value...null is intentionally without value...and undefined variable exit but without value..
2) What is the use of the map() function in JavaScript? How is it different from forEach()?

=>the map is a function and it can be a return value provide it..where forEacht function are not return value ...it works are inside the value
3) What is the difference between == and ===?
=>  (==) that is mean whether are the value same or not ...and its not a check its data types
   where (===) it checks also a data types...
4) What is the significance of async/await in fetching API data?

=>async and await actually works is synchoronously... no body can asynchoronously print it..
5) Explain the concept of Scope in JavaScript (Global, Function, Block).
=>global it actually works in everywhere..
    function is kind of a bridge...when it needs it..it can work smoothly
    block is the function that block the value.